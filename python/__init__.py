# NetworkIntel Python Package
#
# 项目（仓库/发布）版本号。GUI 有独立的 APP_VERSION（见 main_gui.py 与
# python/CHANGELOG.md），二者是不同的版本轴：
#   __version__   —— 项目公开发布版本（GitHub release / tag）
#   APP_VERSION   —— GUI 界面自有版本（独立 changelog）
__version__ = "0.2.1"
